import { useState } from "react"

/*function DarkTheme(){
    const [dark,setDark]= useState(true)
    return <div className={`h-screen ${dark ? "bg-white": "bg-blue-200"}`} >
        <button onClick={()=>{
            setDark(!dark)
        }}>Toggle theme</button>
    </div>
}*/

// in tailwind
